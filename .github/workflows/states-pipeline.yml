name: States pipeline (discover → approve → apply → audit)
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  discover:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20.x'
      - run: node scripts/discover-states.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: state-candidates
          path: db/state-link-candidates.csv

  approve:
    needs: discover
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20.x'
      - uses: actions/download-artifact@v4
        with:
          name: state-candidates
          path: db

      - run: node scripts/auto-approve-states.mjs
      - run: |
          if [ -f scripts/audit-states.mjs ]; then
            node scripts/audit-states.mjs
          else
            npm run audit:states || true
          fi

      - name: Commit & push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add db/states.csv db/state-*.csv || true
          git commit -m "chore(states): pipeline auto-approve + apply + audit" || echo "no changes"
          git push || true
